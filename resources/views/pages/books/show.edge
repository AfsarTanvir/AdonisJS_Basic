<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> Book List </title>

  @vite(['resources/js/app.js'])
</head>
<body>
  <div class="max-w-3xl mx-auto mt-6">
    <nav>
      <a href="{{ route('home') }}">Home</a>
      <a href="{{ route('books.show', { slug: 'book01' }) }}">01</a>
      <a href="{{ route('books.show', { slug: 'book02' }) }}">02</a>
      <a href="{{ route('books.show', { slug: 'book03' }) }}">03</a>
      <a href="{{ route('books.show', { slug: 'book04' }) }}">04</a>
      <a href="{{ route('books.show', { slug: 'book05' }) }}">05</a>
    </nav>

    <h1>Title: {{ md.frontmatter.title }}</h1>
    <h2>Authors: {{ md.frontmatter.authors }}</h2>
    <h3>Topics: {{ md.frontmatter.topics }}</h3>

    {{{ book }}}
  </div>
</body>
</html>